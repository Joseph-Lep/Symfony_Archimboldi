{% extends 'base.html.twig' %}

{% block title %}
  Les derniers bouquins
{% endblock %}

{% block body %}
  <h1 class="text-4xl text-center">Les derniers bouquins</h1>
<ul>
{% for book in books %}
<li>
<a class="hover:underline" href="{{ path('book_list', { id: book.id }) }} {{book.cover}}">{{book.title}}</a>
{{book.title}}
{{book.author}}
{{book.publisher}}
{{book.backcover}}
<li>
</ul>
{% endfor %}

{% endblock %}

